App As appinfo:
    BackEnabled: =true
    OnStart: |+
        =Collect(BudgetsCollect, Budgets)
        
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

